import styled from "styled-components";

const Style = styled.div`
    width: fit-content;
    padding: 22px 32px;
    background-color: ${props => props.theme.pallet.gray300};
`

export default Style;